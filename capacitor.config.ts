import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MHWBG_Companion',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
