import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MHWBG Companion',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
